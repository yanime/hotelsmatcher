/*global define*/
define([
    'underscore',
    'backbone'
], function (_, Backbone) {
    'use strict';

    var ResultModel = Backbone.Model.extend({
        defaults: {
        }
    });

    return ResultModel;
});
